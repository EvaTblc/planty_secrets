<div class="index-container">
  <div class="planty-container">
    <%= image_tag "logo.png", class:"planty-image"%>
  </div>
  <h1>Découvre de nouvelles plantes!</h1>

  <div class="div", data-controller="index-search">
    <div class="mt-4 mb-4 formulaire">
      <%= form_with url: plants_path, method: :get, html: { data: {index_search_target: "form"}}, class: "d-flex" do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Cherchez votre plante...",
          data: {
            index_search_target: "input",
            action: "keyup->index-search#update"
          }
        %>
        <%= submit_tag "Search", class: "btn btn-primary", id: "color-white" %>

      <% end %>
    </div>

    <h2> Dernières recherches 🌿</h2>
    <div class="latest-research-container">
      <%= render "plants/list" %>
  </div>
  <div class="index-favorites">
    <h2> Plantes favorites 🪴 </h2>
    <div class="latest-research-container">
      <div class="plant-row">
        <% @favorites.each do |favorite| %>
          <%= link_to plant_path(favorite) do %>
            <div class="card plant-card">
              <%= cl_image_tag favorite.photo.key, class: "plant-img"%>
              <% end %>
              <div class="card-body plant-body">
                <h3 class="card-title plant-title text-green"><%= favorite.name %></h3>
                <p class="card-text plant-text">France</p>
                <i class="fas fa-heart position-icon"></i>
              </div>
            </div>
          <% end %>
      </div>
    </div>
  </div>
</div>
